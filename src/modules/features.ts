export enum Flags {
  FilterType = "events-filter_by_type",
  FilterTypeVariant = "events-filter_by_type_variant",
  FilterCategoryVariant = "events-filter_by_category_variant",
  FilterTimeVariant = "events-filter_by_time_variant",
}

export enum FilterTypeVariant {
  enabled = "enabled",
  disabled = "disabled",
}

export enum FilterCategoryVariant {
  enabled = "enabled",
  disabled = "disabled",
}

export enum FilterTimeVariant {
  enabled = "enabled",
  disabled = "disabled",
}
